# Copy this to .env and fill in real values. .env is not committed (see .gitignore).

# Groq API key for ai-service (get one at console.groq.com/keys)
GROQ_API_KEY=gsk...

# PostgreSQL credentials (defaults for local docker-compose are already set in the compose file)
DB_USERNAME=taskuser
DB_PASSWORD=taskpass
